<%= form_with(model: timeline_request, local: true) do |form| %>
  <% if timeline_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(timeline_request.errors.count, "error") %> prohibited this timeline_request from being saved:</h2>

      <ul>
        <% timeline_request.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.datetime_select :start_date %> UTC
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.datetime_select :end_date %> UTC
  </div>

  <div class="field">
    <%= form.label :to %>
    <%= form.email_field :to %>
  </div>

  <div class="field">
    <%= form.label :subject %>
    <%= form.text_field :subject %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
